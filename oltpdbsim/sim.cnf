simulation.name=C-T-SWORD-TPCC-IDT-1.0

setup=consistenthash

static.repartitioning=true
hourly.repartitioning=false
threshold.repartitioning=true

workload.aware=true
incremental.repartitioning=true
graphcutbased.repartitioning=true

dynamic.partitioning=false

workload.representation=hgr

stream.collection=false
stream.collector.size.factor=3

associative=false
adaptive=false

transaction.classification=false
transaction.classification.strategy=fcimining

data.migration.strategy=sword

compression.enabled=true
compression.before.setup=true
compression.ratio=6

initial.servers=4
server.ssd=2
server.ssd.capacity=40000
fixed.partitions=64
fixed.partition.capacity=100000
number.of.replicas=1

simulation.period=54000
warmup.period=10800

inverse.of.mean.inter.arrival.time=1
inverse.of.mean.service.time=1000

unique.enable=false
percentage.change.in.workload=0.15
observation.window.size=3600
unique.max.fixed=900
exponential.Avg.Weight=0.5

span.reduction=true
span.reduce=3
lambda=1.0

idt.threshold=0.5