simulation.name=Hourly-Analysis

setup=consistenthash

analysis = true

static.repartitioning=false
hourly.repartitioning=false
threshold.repartitioning=false

workload.variation=false

workload.aware=false
incremental.repartitioning=false
graphcutbased.repartitioning=false

workload.representation=hgr

transaction.classification=false
transaction.classification.strategy=basic

data.migration.strategy=analysis

compression.enabled=false
compression.before.setup=false
compression.ratio=6

initial.servers=4
server.capacity=100000
fixed.partitions=32
number.of.replicas=1

simulation.period=21600
warmup.period=10800

inverse.of.mean.inter.arrival.time=1
inverse.of.mean.service.time=1000

percentage.change.in.workload=0.15
adjustment=0.6
observation.window.size=3600
unique.max.fixed=900
exponential.Avg.Weight=0.4

admin.idt.threshold=0.5

methodX.decision.priority=0.5
rbsta.span.reduction=1